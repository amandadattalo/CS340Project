<!-- NAV -->
<nav>
    <ul>
        <li id = "navli"><a href="/">Home</a></li>
        <li id = "navli"><a href="books">Books</a></li>
        <li id = "navli"><a href="authors">Authors</a></li>
        <li id = "navli"><a href="series">Series</a></li>
        <li id = "navli"><a href="genres">Genres</a></li>
        <li id = "navli"><a href="intersection_tables">Book and Series to Authors</a></li>
    </ul>
</nav>

<!-- Header -->
<h1>Authors</h1>

<!-- Table -->
{{!-- Create a table --}}
<table id="authors_table">
    {{!-- Header section --}}
    <thead>
        <tr>
        {{#each data.[0]}}
            {{#unless @first}}
                <th>{{@key}}</th>
            {{/unless}}
        {{/each}}
        <th>Edit</th>
        <th>Delete</th>
    </tr>
    </thead>
    {{!-- Body section --}}
    <tbody>
        {{#each data}}
        <tr data_value = {{this.author_id}}>
            <td>{{this.First}}</td>
            <td>{{this.Last}}</td>
            <td><button id="edit" onclick="location.href='/edit_authors?author_id={{this.author_id}}'">Edit</button></td>
            <td><button id='delete', onclick="deleteAuthor({{this.author_id}})">Delete</button>
        </tr>
        {{/each}}
    </tbody>
</table>

{{!-- Connect to delete js file --}}
<script src="js/delete_author.js"></script>

<!-- Button to add author -->
<button onclick="location.href='add_authors'" id="blue_button">Add Author</button> 

